class opal::proxy ($opal_http_port='8080', 
                   $opal_https_port='8443', 
                   $target_http_port='',
                   $target_https_port=''){

 

 #  # create apache vhost for production instance
#  apache::vhost{$fqdn:
#     port     => $port,
#     docroot  => $docroot,
#     priority => 10
#   }


}
