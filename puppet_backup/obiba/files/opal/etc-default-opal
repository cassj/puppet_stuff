# Defaults for opal initscript
# sourced by /etc/init.d/opal
# installed at /etc/default/opal by the maintainer scripts

# pulled in from the init script; makes things easier.
NAME=opal

# location of java
JAVA=/usr/bin/java

# arguments to pass to java
JAVA_ARGS="-Xmx2048m -XX:MaxPermSize=256M -XX:+UseConcMarkSweepGC -XX:+UseParNewGC -XX:+CMSParallelRemarkEnabled"

# ulimit
# Increase maximum open files for the opal process
# Comment this out to use the system default
MAXOPENFILES=16384

# user id (or id:gid) to be run as
OPAL_USER=obiba

# location of the opal distribution file
OPAL_DIST=/opt/obiba/opal/opal-server/opal-server-1.5.0

# Opal home location
OPAL_HOME=/opt/obiba/opal/opal-server/opal-server-1.5.0

# Opal log directory or syslog facility
OPAL_LOG=/opt/obiba/opal/opal-server/opal-server-1.5.0/logs/opal.log

# arguments to pass to opal.
OPAL_ARGS="--upgrade"
